import { PlaceholderPanel, Screen } from "@/components/Screen";

export default function SavingsPage() {
  return (
    <Screen title="Savings" description="Savings product placeholder for a future module.">
      <PlaceholderPanel title="Savings API pending" text="No savings backend contract exists yet." />
    </Screen>
  );
}
